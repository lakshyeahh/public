const videoCards = [...document.querySelectorAll(".navvideo")];

videoCards.forEach((item) => {
  item.addEventListener("mousover", () => {
    let video = item.children[1];
    video.play();
  });
  item.addEventListener("mouseleave", () => {
    let video = item.children[1];
    video.pause();
  });
});
